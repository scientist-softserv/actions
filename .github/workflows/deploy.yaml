name: "Deploy"
on:
  workflow_call:
    inputs:
      k8s-release-name:
        required: false
        type: string
      k8s-namespace:
        required: false
        type: string
      solr:
        description: "Add if your solr image is from the registry"
        required: false
        type: boolean
      tag:
        description: "Tag to override default of current branch sha"
        required: false
        type: string
      helm-values-file-name:
        description: "Specify a values file name before the `-deploy.tmpl.yaml`"
        required: false
        type: string

env:
  REGISTRY: ghcr.io

jobs:
  deploy:
    runs-on: self-hosted
    container: dtzar/helm-kubectl:3.9.4
    environment: ${{ inputs.environment }}
    env:
      HELM_EXPERIMENTAL_OCI: 1
      HELM_RELEASE_NAME: ${{ github.event.repository.name }}-${{ inputs.environment }}
      KUBE_NAMESPACE: ${{ github.event.repository.name }}-${{ inputs.environment }}
      HELM_EXTRA_ARGS: >
        --values ops/${{ inputs.environment }}-deploy.yaml
      KUBECONFIG_FILE: ${{ secrets.KUBECONFIG_FILE }}
      KUBECONFIG: ./kubeconfig.yml
      DATABASE_PASSWORD: ${{ secrets.DATABASE_PASSWORD }}
      DB_PASSWORD: ${{ secrets.DB_PASSWORD }}
      GOOGLE_FONTS_KEY: ${{ secrets.GOOGLE_FONTS_KEY }}
      GOOGLE_OAUTH_PRIVATE_KEY_SECRET: ${{ secrets.GOOGLE_OAUTH_PRIVATE_KEY_SECRET }}
      HBK_PAPERTRAIL_API_TOKEN: ${{ secrets.HBK_PAPERTRAIL_API_TOKEN }}
      HBK_SENDGRID_PASSWORD: ${{ secrets.HBK_SENDGRID_PASSWORD }}
      HBK_STRIPE_SECRET_KEY: ${{ secrets.HBK_STRIPE_SECRET_KEY }}
      IA_PASSWORD: ${{ secrets.IA_PASSWORD }}
      LJK_SENDGRID_PASSWORD: ${{ secrets.LJK_SENDGRID_PASSWORD }}
      LJK_SQUARE_ACCESS_TOKEN: ${{ secrets.LJK_SQUARE_ACCESS_TOKEN }}
      LJK_SQUARE_WEBHOOK_SIGNATURE_KEY: ${{ secrets.LJK_SQUARE_WEBHOOK_SIGNATURE_KEY }}
      LJK_STRIPE_SECRET_KEY: ${{ secrets.LJK_STRIPE_SECRET_KEY }}
      MAIL_PASS: ${{ secrets.MAIL_PASS }}
      POSTGRES_PASSWORD: ${{ secrets.POSTGRES_PASSWORD }}
      REDIS_PASSWORD: ${{ secrets.REDIS_PASSWORD }}
      SECRET_KEY_BASE: ${{ secrets.SECRET_KEY_BASE }}
      SMTP_PASSWORD: ${{ secrets.SMTP_PASSWORD }}
      SOLR_ADMIN_PASSWORD: ${{ secrets.SOLR_ADMIN_PASSWORD }}
      SQUARE_ACCESS_TOKEN: ${{ secrets.SQUARE_ACCESS_TOKEN }}
      SQUARE_WEBHOOK_SIGNATURE_KEY: ${{ secrets.SQUARE_WEBHOOK_SIGNATURE_KEY }}
    steps:
      - id: setup
        name: Setup
        uses: scientist-softserv/actions/setup-env@self-hosted-runner
        with:
          tag: ${{ inputs.tag }}
      - name: Checkout code
        uses: actions/checkout@v3
      - name: Setup tmate session
        uses: mxschmitt/action-tmate@v3
        if: ${{ github.event_name == 'workflow_dispatch' && inputs.debug_enabled }}
        with:
          limit-access-to-actor: true
      - name: Do deploy with solr registry image
        if: ${{ inputs.solr }}
        run: |
          echo $KUBECONFIG_FILE | base64 -d > $KUBECONFIG;
          DOLLAR=$ envsubst < ops/${{ inputs.helm-values-file-name || inputs.environment }}-deploy.tmpl.yaml > ops/${{ inputs.helm-values-file-name || inputs.environment }}-deploy.yaml;
          export DEPLOY_TAG=${TAG};
          export DEPLOY_IMAGE=ghcr.io/${REPO_LOWER};
          export WORKER_IMAGE=ghcr.io/${REPO_LOWER}/worker;
          export SOLR_IMAGE=ghcr.io/${REPO_LOWER}/solr;
          ./bin/helm_deploy ${{ inputs.k8s-release-name || format('{0}-{1}', github.event.repository.name, inputs.environment) }} ${{ inputs.k8s-namespace || format('{0}-{1}', github.event.repository.name, inputs.environment) }}
      - name: Do deploy
        run: |
          echo $KUBECONFIG_FILE | base64 -d > $KUBECONFIG;
          DOLLAR=$ envsubst < ops/${{ inputs.helm-values-file-name || inputs.environment }}-deploy.tmpl.yaml > ops/${{ inputs.helm-values-file-name || inputs.environment }}-deploy.yaml;
          export DEPLOY_TAG=${TAG};
          export DEPLOY_IMAGE=ghcr.io/${REPO_LOWER};
          export WORKER_IMAGE=ghcr.io/${REPO_LOWER}/worker;
          ./bin/helm_deploy ${{ inputs.k8s-release-name || format('{0}-{1}', github.event.repository.name, inputs.environment) }} ${{ inputs.k8s-namespace || format('{0}-{1}', github.event.repository.name, inputs.environment) }}
